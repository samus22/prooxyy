// -- Firebase Cloud Firestore
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:prooxyy_events/models/user.dart';

abstract class Dao<T> {
    // final String COLLECTION = 'entity';

    // Future<void> add(T entity);
    // Future<DocumentReference> get(String id);
    // Future<DocumentSnapshot> getAsSnapshot(String id);
    // Future<Query> getAll();
    // Future<QuerySnapshot> getAllAsSnapshot();
    // Future<void> delete(String id);
    // Future<void> update(String id, T entity);
}

abstract class AuthRepo {
    // void login(String email, String password);
    // void logout();
    // void forgotPassword();
    // void resetPassword();
    // void register(User user);
}